
type Idea {
    id: ID!
    idea: String!
    comments: [Comment!]
    description: String!
    author: User
    upvotes: Int
    downvotes: Int
    createdAt: String!
    updatedAt: String!
}

type Query {
    ideas(page: Int, newest: Boolean): [Idea!]
}